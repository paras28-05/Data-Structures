#include <iostream>
#include <vector>
#include <unordered_map>
#include <queue>

using namespace std;
template <typename T>

vector <vector<int>> print_adj_list(int no_nodes , int no_edges , vector<vector<int>> &edges){
    vector<int> ans[no_nodes];

    for (int i=0; i<no_edges ; i++){
        int u = edges[i][0];
        int v = edges[i][1];

        ans[u]
    }
}

int main(){

    return 0;
}